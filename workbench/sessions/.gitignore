# Live session records stay local; only checkpoints/ is durable evidence.
grilling/*
!grilling/.gitkeep
handoffs/*
!handoffs/.gitkeep
# Legacy notepad path a stale installed skill may still write; never tracked.
grilling diary/
